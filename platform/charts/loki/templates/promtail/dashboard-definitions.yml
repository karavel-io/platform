apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-promtail
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: promtail
    {{- include "loki.labels" . | nindent 4 }}
data:
  promtail.json: |
  {{ .Files.Get "dashboards/promtail.json" | nindent 4 }}
