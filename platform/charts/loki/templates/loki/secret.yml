apiVersion: v1
kind: Secret
metadata:
  name: loki
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: loki
data:
  loki.yaml: {{ tpl (.Files.Get "configs/loki.yaml") . | b64enc}}
