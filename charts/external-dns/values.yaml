domainFilter: ""

provider: cloudflare

cloudflare:
  proxied: true
  zoneId: ""
  secret:
    backend: secretsManager
    key: ""
    property: ""

argocd:
  namespace: argocd
  git:
    repo: git@github.com:example/example.git
    path: "manifests/external-dns/templates"
    rev: HEAD

