- hosts: localhost
  vars_files:
    - vars.yml
  tasks:
    - import_role:
        name: mikamai.karavel.core
    - import_role:
        name: mikamai.karavel.argocd
    - import_role:
        name: mikamai.karavel.nginx
    - import_role:
        name: mikamai.karavel.argo_apps
    - import_role:
        name: mikamai.karavel.bootstrap_kustomization
