---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "cert-manager.name" . }}-cainjector-psp
  labels:
    app.kubernetes.io/name: {{ include "cert-manager.name" . }}-cainjector-psp
    {{- include "cert-manager.labels" . | nindent 4 }}
    app.kubernetes.io/component: cainjector
rules:
- apiGroups: ['policy']
  resources: ['podsecuritypolicies']
  verbs:     ['use']
  resourceNames:
  - {{ include "cert-manager.name" . }}-cainjector
