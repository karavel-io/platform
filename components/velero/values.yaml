prometheus:
  enable: false

aws:
  eksRole:
  iamRole:

backups:
  enable: true
  provider:
  s3:
    bucket:
    endpoint:
    region:
    prefix:
    caCert:
    encrypted: true
    insecure: false
    pathStyle: false
    accessKeySecret:
    secretKeySecret:

snapshots:
  enable: true
  provider:
  region:

restic:
  enable: false
  podVolumePath: /var/lib/kubelet/pods
  privileged: false

credentialsSecret:
  backend:
  key:
  property:

credentialsPath:
